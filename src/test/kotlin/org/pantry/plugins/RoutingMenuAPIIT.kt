package org.pantry.plugins

import io.ktor.client.request.*
import io.ktor.client.statement.*
import io.ktor.http.*
import io.ktor.server.testing.testApplication
import org.junit.jupiter.api.Test
import kotlin.test.assertEquals

class RoutingMenuAPIIT {
/*
    @Test
    fun `GET menu returns 200 and JSON body`() = testApplication {
        val response = client.get("/menu")

        assertEquals(HttpStatusCode.OK, response.status)
        println(response.bodyAsText())
    }

    @Test
    fun `GET user settings returns 200 and JSON body`() = testApplication {
        val response = client.get("/userSettings")

        assertEquals(HttpStatusCode.OK, response.status)
        println(response.bodyAsText())
    }

    @Test
    fun `GET app settings returns 200 and JSON body`() = testApplication {
        val response = client.get("/appSettings")

        assertEquals(HttpStatusCode.OK, response.status)
        println(response.bodyAsText())
    }

    @Test
    fun `GET returns 404 on wrong address`() = testApplication {
        val response = client.get("/wrong")

        assertEquals(HttpStatusCode.NotFound, response.status)
    }
*/
}