ktor:
  application:
    modules:
      - org.pantry.ApplicationKt.module
  deployment:
    port: 8080

db:
  url: ${DB_URL:"jdbc:postgresql://localhost:5432/pantryman"}
  user: ${DB_USER:"user"}
  password: ${DB_PASSWORD:"password"}